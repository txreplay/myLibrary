		<nav class="navbar navbar-default">
			<div class="container-fluid">
		    	<!-- Brand and toggle get grouped for better mobile display -->
		    	<div class="navbar-header">
		      		<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
				        <span class="sr-only">Toggle navigation</span>
				        <span class="icon-bar"></span>
				        <span class="icon-bar"></span>
				        <span class="icon-bar"></span>
		      		</button>
		      		<a class="navbar-brand" href="{{ path('index') }}">MyLibrary</a>
		    	</div>

			    <div class="navbar-collapse collapse navbar-responsive-collapse" id="bs-example-navbar-collapse-1">
		      		<ul class="nav navbar-nav">
				        <li><a href="{{ path('movies') }}">Films</a></li>
				        <li><a href="{{ path('actors') }}">Acteurs</a></li>
				        <li class="dropdown">
		          			<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Genres <span class="caret"></span></a>
	          				<ul class="dropdown-menu" role="menu">
					            {% for genre in genres %}
								    <li><a href="{{ path('genre', { 'id': genre.id}) }}">{{ genre.name }}</a></li>
								{% endfor %}
	          				</ul>
		        		</li>
		        		<li><a href="{{ path('movie', { 'id': random(343118) }) }}">Aléatoire</a></li>
		      		</ul>
		      		<ul class="nav navbar-nav navbar-right">
				        {% if app.user != null %}
					        <li class="dropdown">
					        	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ app.user.username }}<span class="caret"></span></a>
					          	<ul class="dropdown-menu" role="menu">
						            {#<li><a href="{{ path('fos_user_profile_show') }}">Voir mon profil</a></li>#}
						            {#<li><a href="{{ path('fos_user_profile_edit') }}">Modifier mon profil</a></li>#}
						            <li class="divider"></li>
						            {#<li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>#}
					          	</ul>
				        	</li>
				        {% else %}
				        	{#<li><a href="{{ path('fos_user_registration_register') }}">Inscription</a></li>#}
				        	{#<li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>#}
			        	{% endif %}
      				</ul>
		      		<form class="navbar-form navbar-right" role="search" id="searchForm" action="{{ path("search") }}" method="post">
						{{ form_widget(form.movieRequest, {'attr': {'class': 'form-control', 'placeholder': 'Recherchez un film...'}}) }}
		        		{{ form_rest(form) }}
		        		{#<button type="submit" class="btn btn-default">Rechercher</button>#}
		      		</form>
		    	</div>
		  	</div>
		</nav>